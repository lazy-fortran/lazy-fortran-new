(schema l3-print-expression-power-variable-value-v0
  (primitive name)
  (primitive int)
  (enum result accepted rejected)
  (record source-case
    (id name)
    (path name)
    (sha256 name)
    (expected result))
  (record source-evidence
    (document name)
    (pdf-sha256 name)
    (standardir-path name)
    (standardir-sha256 name)
    (rules name)
    (pages int))
  (record print-expression-power-variable-value-contract
    (property name)
    (positive source-case)
    (positive-wide source-case)
    (negative-missing-operand source-case)
    (negative-variable-exponent source-case)
    (negative-negative-exponent source-case)
    (negative-write source-case)
    (evidence source-evidence)))
