# M3 Core 0 next-property selection after C726

Status: `PASS` for selection only. This report does not promote C731 or any
semantic fact. It records the next bounded-property handoff from the exact
retained E0181 residual.

## Selection

The promoted set contains C1106, C702, C601, C603, C721, C725, C718, C723,
C729, C719, C738, C1579, C1586, C717, C720, C722, C724 and C726. The exact
partition reports 153 outside-promoted rows: 88 `disputed` and 65
`unwitnessed`. Its first row is `C731@1`.

The selected row is C731@1. Its retained proposal is model-origin input and
is not accepted evidence. The source-backed property selected for the next
bounded contract is:

> In a character statement function or a statement-function dummy argument of
> type CHARACTER, the specified length shall be a constant expression.

The next contract may use typed length-form states such as
`constant-expression`, `non-constant-expression` and `unknown`, together with
the two source-named contexts. It must derive those states from its own
source-backed fixture and must not treat the retained model witnesses as
semantic evidence.

## Normative and StandardIR evidence

The pinned source is J3/24-007, PDF SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, with
canonical-text SHA-256
`1cf538329c57e4f617adb36f2c7cd91a5a5561c78bcce16ec96f7ff1a9979f9e` and
page-index SHA-256
`49406a5aecf423555662643f07f6c2bdf72dd3df3954862231afa31505e18929`.

C731 is canonical lines 3469--3470, byte span `219036:167` (end byte
`219203`), on page 85. The source text is split across the two canonical
lines but forms one sentence. The prior model-origin row labels the source as
page 84; the pinned page index and byte offsets control this selection and
place it on page 85.

The already represented grammar shape is StandardIR R721 (`char-selector`,
page 84, byte start 217200, length 251, occurrence 71). It cites the same
canonical source hash; the pinned StandardIR file has SHA-256
`106389186689ae819783ab6742ba4a469f8d1a84ce3bbf25e9baf98a32cf25c2`.

## Reproduction

The residual counts, first row and row-key list are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717","C720","C722","C724","C726"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), first_row: .[0].row_key, row_keys: map(.row_key)}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The selected row and retained proposal are regenerated by:

```text
jq -c 'select(.row_key == "C731@1")' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
jq -c 'select(.row_key == "C731@1")' .cache/runs/E0181/R000002/analysis/merged/selected-rows.jsonl
```

The source and existing StandardIR binding are regenerated by:

```text
nl -ba .cache/runs/E0001/R000003/j3-24-007.canonical.txt | sed -n '3469,3470p'
sed -n '87,91p' .cache/runs/E0001/R000003/j3-24-007.pages.index
sed -n '72p' .cache/runs/E0171/R000433-provenance-replay/standardir.sx
```

The selection is mechanical, makes zero model calls and promotes no semantic
fact. The next executable task is one bounded C731 constant-expression
contract; it must remain smaller than full constant-expression evaluation and
must not start general parsing or semantic analysis.
