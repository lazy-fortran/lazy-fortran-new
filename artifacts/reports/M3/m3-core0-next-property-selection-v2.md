# M3 next bounded-property selection v2

Selection evidence is generated from the post-C717 outside-promoted E0181
residual by the exact verifier in `TASK_POOL.yaml`. It reports 157 rows: 90
`disputed` and 67 `unwitnessed`. C720 is the first remaining row in normative
order with an already represented StandardIR shape, R708
`int-literal-constant`.

The selected contract is D0141/E0190: J3-24-007 C720, page 80, canonical line
3298. Its typed candidate carries
`representation_method = absent | present | unknown`. The deterministic
oracle returns `ACCEPTED` for `present`, `REJECTED` for `absent`, and
`UNRESOLVED` for `unknown`.

The source evidence is the pinned PDF, canonical text, page index and
StandardIR R708 row. The next implementation gate must reject source,
page-index, StandardIR, semantic-item and contract-identity mutations. It
must not parse Fortran, evaluate kind expressions, inspect processor
capabilities or consume model output.

The selection and residual counts are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), row_keys: map(.row_key)}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The next executable task is the separate C720 implementation and replay gate.
Selection does not promote C720.
