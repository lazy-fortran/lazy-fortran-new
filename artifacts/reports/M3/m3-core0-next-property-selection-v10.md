# M3 Core 0 next-property selection after C735

Status: `PASS` for selection only. This report selects the next bounded
property; it does not promote C743 or any semantic fact.

## Selection

The exact twenty-two-entry promoted partition leaves 149 residual witness
rows: 85 `disputed` and 64 `unwitnessed`. The first residual row is
`C743@1`. The retained row is model-origin input only; no model execution is
used as evidence.

The selected source-backed property for the next bounded contract is:

> The same `private-or-sequence` shall not appear more than once in a given
> `derived-type-def`.

The next contract shall use these typed candidate fields:

```text
private-or-sequence-occurrence: none | single | duplicate | unknown
context: derived-type-def | other | unknown
```

Its deterministic boundary is:

```text
none or single, context=derived-type-def  ACCEPTED
duplicate, context=derived-type-def       REJECTED
otherwise                                 UNRESOLVED
```

The positive neighbours are no occurrence and one occurrence. The negative
neighbour is a duplicate occurrence. Unknown occurrence or context is
unresolved. This is a typed uniqueness relation only; it does not parse a
derived-type definition, distinguish the meaning of PRIVATE from SEQUENCE,
validate the R729 alternatives, resolve type names or claim full C743/M3
semantics.

## Normative and StandardIR evidence

The pinned source is J3/24-007, PDF SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, with
canonical-text SHA-256
`1cf538329c57e4f617adb36f2c7cd91a5a5561c78bcce16ec96f7ff1a9979f9e` and
page-index SHA-256
`49406a5aecf423555662643f07f6c2bdf72dd3df3954862231afa31505e18929`.

C743 is canonical line 3637, byte span `230736:105`, on printed page 89. The
exact source bytes are:

```text
7 C743 (R726) The same private-or-sequence shall not appear more than once in a given derived-type-def.
```

The page index places this span within page 89, `start 230288`, `length 2594`.
The existing StandardIR shapes are R726, `derived-type-def`, page 88, byte
span `229000:177`, occurrence 76, and R729, `private-or-sequence`, page 89,
byte span `230663:72`, occurrence 79. R726 contains a zero-or-more repeat of
R729; R729 has the two source alternatives `private-components-stmt` and
`sequence-stmt`. Both cite the pinned canonical source hash. The pinned
StandardIR file has SHA-256
`106389186689ae819783ab6742ba4a469f8d1a84ce3bbf25e9baf98a32cf25c2`.

The retained C743 proposal reports model-self-consistency cases, including
single and duplicate occurrences. Those observations are not accepted as
semantic evidence; the typed boundary above is the only selected contract.

## Reproduction

The residual counts and first row are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717","C720","C722","C724","C726","C731","C732","C733","C735"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), first_row: .[0].row_key, first_constraint: .[0].constraint_id}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The selected row is regenerated by:

```text
jq -c 'select(.row_key == "C743@1")' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The source, page and StandardIR bindings are regenerated by:

```text
nl -ba .cache/runs/E0001/R000003/j3-24-007.canonical.txt | sed -n '3637p'
python3 - <<'PY'
from pathlib import Path
lines = Path('.cache/runs/E0001/R000003/j3-24-007.canonical.txt').read_bytes().splitlines(keepends=True)
print('start', sum(map(len, lines[:3636])), 'length', len(lines[3636]))
PY
rg -n '^page 89 ' .cache/runs/E0001/R000003/j3-24-007.pages.index
sed -n '77,80p' .cache/runs/E0171/R000433-provenance-replay/standardir.sx
```

The selection is mechanical, makes zero model calls and promotes no semantic
fact. The next executable task is one bounded C743 typed uniqueness oracle.
