# M3 Core 0 next-property selection after C724

Status: `PASS` for selection only. This report does not promote C726 or any
semantic fact. It records the next bounded-property handoff from the exact
retained E0181 residual.

## Selection

The promoted set contains C1106, C702, C601, C603, C721, C725, C718, C723,
C729, C719, C738, C1579, C1586, C717, C720, C722 and C724. The exact
partition reports 154 outside-promoted rows: 88 `disputed` and 66
`unwitnessed`. Its first row is `C726@1`.

The selected row is C726@1. Its retained proposal describes the candidate
property as: a `type-param-value` of `*` is permitted only in a bounded set of
contexts. The proposal is model-origin input and is not accepted evidence;
the next contract must derive its candidate states and outcomes from the
normative source below.

## Normative and StandardIR evidence

The pinned source is J3/24-007, PDF SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, with
canonical-text SHA-256
`1cf538329c57e4f617adb36f2c7cd91a5a5561c78bcce16ec96f7ff1a9979f9e` and
page-index SHA-256
`49406a5aecf423555662643f07f6c2bdf72dd3df3954862231afa31505e18929`.

C726 begins at canonical lines 3453--3457 on page 84 and continues at lines
3460--3461 on page 85. Its canonical byte span is 217828:422 and therefore
crosses the page-84/page-85 boundary. The source text names these permitted
contexts: dummy argument; named constant; an `ALLOCATE` type-spec whose
allocate-objects are dummy arguments of assumed-length `CHARACTER`; a type
guard type-spec or derived-type-spec; and an external-function character
result length parameter.

The already represented grammar shapes are StandardIR R721 (`char-selector`,
page 84, byte start 217200, length 251, occurrence 71), R722
(`length-selector`, page 84, byte start 217452, length 83, occurrence 72),
and R723 (`char-length`, page 84, byte start 217536, length 70, occurrence
73). All three cite the canonical source hash above; the pinned StandardIR
file has SHA-256
`106389186689ae819783ab6742ba4a469f8d1a84ce3bbf25e9baf98a32cf25c2`.

The page-crossing span is a required negative control for the next contract:
an implementation that checks only page 84 must fail closed. The next contract
must remain smaller than full type-parameter semantic analysis and must not
parse Fortran, infer context from arbitrary source, or use model output to
promote a fact.

## Reproduction

The residual counts, first row and row-key list are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717","C720","C722","C724"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), first_row: .[0].row_key, row_keys: map(.row_key)}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The selected-row proposal and its retained witness status are regenerated by:

```text
jq -c 'select(.constraint_id == "C726")' .cache/runs/E0181/R000002/analysis/merged/selected-rows.jsonl
jq -c 'select(.row_key == "C726@1")' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

Source lines and page boundaries are regenerated by:

```text
nl -ba .cache/runs/E0001/R000003/j3-24-007.canonical.txt | sed -n '3453,3461p'
sed -n '87,91p' .cache/runs/E0001/R000003/j3-24-007.pages.index
sed -n '72,74p' .cache/runs/E0171/R000433-provenance-replay/standardir.sx
```

Selection is mechanical, makes zero model calls, and promotes no semantic
fact. The next executable task is the single bounded C726 contract, subject to
a new decision and an independent deterministic oracle.
