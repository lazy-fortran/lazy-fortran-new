# M3 Core 0 next-property selection after C731

Status: `PASS` for selection only. This report does not promote C732 or any
semantic fact. It records the next bounded-property handoff from the pinned
E0181 residual.

## Selection

The exact nineteen-entry promoted set leaves 152 residual witness rows: 88
`disputed` and 64 `unwitnessed`. The first residual row is `C732@1`.

The retained C732 proposal is model-origin input and is not accepted evidence.
The selected source-backed property for the next bounded contract is:

> For a `char-literal-constant`, the value of `kind-param` shall specify a
> representation method that exists on the processor.

The next contract shall use a typed candidate with these states:

- `kind-param-state`: `processor-supported`, `processor-unsupported` or
  `unknown`;
- `context`: `char-literal-constant`, `other` or `unknown`.

Its deterministic oracle is `ACCEPTED` only for a supported kind parameter in
the source-named character-literal context, `REJECTED` for an unsupported kind
parameter in that context, and `UNRESOLVED` for unknown states or other
contexts. This is a bounded relation over typed input; it is not processor
introspection, literal parsing, constant evaluation or semantic analysis.

## Normative and StandardIR evidence

The pinned source is J3/24-007, PDF SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, with
canonical-text SHA-256
`1cf538329c57e4f617adb36f2c7cd91a5a5561c78bcce16ec96f7ff1a9979f9e` and
page-index SHA-256
`49406a5aecf423555662643f07f6c2bdf72dd3df3954862231afa31505e18929`.

C732 is canonical line 3493, byte span `221195:107` (end byte `221302`), on
printed page 85. The exact source bytes are:

```text
34 C732 (R724) The value of kind-param shall specify a representation method that exists on the processor.
```

The existing StandardIR shape is R724, `char-literal-constant`, page 85,
byte start 221076, byte length 118, occurrence 74. It cites the same
canonical source hash. The pinned StandardIR file has SHA-256
`106389186689ae819783ab6742ba4a469f8d1a84ce3bbf25e9baf98a32cf25c2`.

## Reproduction

The residual counts and first row are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717","C720","C722","C724","C726","C731"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), first_row: .[0].row_key, first_constraint: .[0].constraint_id}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The selected row and retained proposal are regenerated by:

```text
jq -c 'select(.row_key == "C732@1")' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
jq -c 'select(.row_key == "C732@1")' .cache/runs/E0181/R000002/analysis/merged/selected-rows.jsonl
```

The source and existing StandardIR binding are regenerated by:

```text
nl -ba .cache/runs/E0001/R000003/j3-24-007.canonical.txt | sed -n '3493p'
sed -n '87,91p' .cache/runs/E0001/R000003/j3-24-007.pages.index
rg '^\\(syntax R724 ' .cache/runs/E0171/R000433-provenance-replay/standardir.sx
```

The selection is mechanical, makes zero model calls and promotes no semantic
fact. The next executable task is one bounded C732 kind-parameter
representation-method contract. It must remain smaller than processor
capability discovery, literal parsing or general semantic analysis.
