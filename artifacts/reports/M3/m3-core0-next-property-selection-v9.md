# M3 Core 0 next-property selection after C733

Status: `PASS` for selection only. This report does not promote C735 or any
semantic fact. It records the next bounded-property handoff from the pinned
E0181 residual.

## Selection

The exact twenty-one-entry promoted partition leaves 150 residual witness
rows: 86 `disputed` and 64 `unwitnessed`. The first residual row is
`C735@1`. The retained row is model-origin input only; no model execution is
used as evidence.

The selected source-backed property for the next bounded contract is:

> The same `type-attr-spec` shall not appear more than once in a given
> `derived-type-stmt`.

The next contract shall use these typed candidate fields:

```text
type-attr-occurrence: none | distinct | duplicate | unknown
context:              derived-type-stmt | other | unknown
```

Its deterministic boundary is:

```text
none or distinct, context=derived-type-stmt  ACCEPTED
duplicate, context=derived-type-stmt         REJECTED
otherwise                                    UNRESOLVED
```

The positive neighbours are no attribute, one attribute and multiple distinct
attributes. The negative neighbour is a repeated attribute. Unknown occurrence
or context is unresolved. This is a typed uniqueness relation only; it does
not parse a derived-type statement, resolve attribute names, validate the
individual attribute alternatives or perform general semantic analysis.

## Normative and StandardIR evidence

The pinned source is J3/24-007, PDF SHA-256
`7371e889f231cfb0316d30365d5083fb5af34cbb6d5f7cb1e01855c73021bfa2`, with
canonical-text SHA-256
`1cf538329c57e4f617adb36f2c7cd91a5a5561c78bcce16ec96f7ff1a9979f9e` and
page-index SHA-256
`49406a5aecf423555662643f07f6c2bdf72dd3df3954862231afa31505e18929`.

C735 is canonical line 3620, byte span `229534:101`, on printed page 88. The
exact source bytes are:

```text
22 C735 (R727) The same type-attr-spec shall not appear more than once in a given derived-type-stmt.
```

The page index places this span within page 88, `start 228013`, `length 2274`.
The existing StandardIR shapes are R727, `derived-type-stmt`, page 88,
byte span `229178:108`, occurrence 77, and R728, `type-attr-spec`, page 88,
byte span `229287:102`, occurrence 78. Both cite the pinned canonical source
hash. The pinned StandardIR file has SHA-256
`106389186689ae819783ab6742ba4a469f8d1a84ce3bbf25e9baf98a32cf25c2`.

The retained C735 proposal reports four model-self-consistency cases, including
the duplicate-attribute negative neighbour and the no-attribute positive
neighbour. Those observations are not accepted as semantic evidence; the
typed boundary above is the only selected contract.

## Reproduction

The residual counts and first row are regenerated by:

```text
jq -s -c 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586","C717","C720","C722","C724","C726","C731","C732","C733"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), first_row: .[0].row_key, first_constraint: .[0].constraint_id}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The selected row is regenerated by:

```text
jq -c 'select(.row_key == "C735@1")' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The source, page and StandardIR bindings are regenerated by:

```text
nl -ba .cache/runs/E0001/R000003/j3-24-007.canonical.txt | sed -n '3620p'
python3 - <<'PY'
from pathlib import Path
lines = Path('.cache/runs/E0001/R000003/j3-24-007.canonical.txt').read_bytes().splitlines(keepends=True)
print('start', sum(map(len, lines[:3619])), 'length', len(lines[3619]))
PY
sed -n '93p' .cache/runs/E0001/R000003/j3-24-007.pages.index
sed -n '78,79p' .cache/runs/E0171/R000433-provenance-replay/standardir.sx
```

The selection is mechanical, makes zero model calls and promotes no semantic
fact. The next executable task is one bounded C735 typed uniqueness oracle.
