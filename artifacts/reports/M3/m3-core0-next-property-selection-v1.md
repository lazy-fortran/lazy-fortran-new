# M3 next bounded-property selection

Selection evidence is generated from the outside-promoted E0181 residual by
the exact verifier in `TASK_POOL.yaml`. That verifier reports 158 rows: 91
`disputed` and 67 `unwitnessed`. C717 is the first outside-promoted row in
normative order with an already represented StandardIR shape, R706
`kind-selector`.

The selected contract is D0139/E0189: J3-24-007 C717, page 80, canonical
lines 3263--3264. Its candidate has only
`kind_value = negative | nonnegative | unknown` and
`representation_method = absent | present | unknown`. The deterministic
oracle returns `ACCEPTED` for `(nonnegative, present)`, `REJECTED` for either
known violation, and `UNRESOLVED` when either relevant state is unknown.

The exact source evidence is the pinned PDF, canonical text, page index and
StandardIR R706 row. The independent C717 validator rejects source,
page-index, StandardIR, semantic-item and contract-identity mutations. It
does not parse Fortran, evaluate expressions, inspect processor capabilities,
call a model or promote semantic facts.

The selection and residual counts are regenerated by:

```text
jq -s 'def promoted: ["C1106","C702","C601","C603","C721","C725","C718","C723","C729","C719","C738","C1579","C1586"]; def is_promoted($id): any(promoted[]; . == $id); map(select((.status == "disputed" or .status == "unwitnessed") and (is_promoted(.constraint_id) | not))) | {rows: length, by_status: (group_by(.status) | map({status: .[0].status, count: length})), row_keys: map(.row_key)}' .cache/runs/E0181/R000002/analysis/witness/witnesses.jsonl
```

The implementation gate is the separate exact command
`tests/e2e/run-m3-c717.sh --fresh`; selection does not promote C717.
