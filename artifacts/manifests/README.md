# Central trace manifests

Commit only small, reviewed manifests that identify a producer commit,
contract revisions, inputs, outputs, oracle and hashes. Large payloads stay in
the ignored cache and are referenced here by path and hash.
