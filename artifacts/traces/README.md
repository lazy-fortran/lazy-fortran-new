# Central integration traces

Trace manifests for cross-repository slices belong here. A trace is not a
PASS by itself; `scripts/verify_active_milestone.sh` must consume it together
with the declared oracle.
