# E0015 report

All counts, hashes and membership sets below are regenerated by
`research/experiments/E0015-can-core-0-feature-eligibility-prune-exc/analyse.sh`.
The input is the pinned E0014 dependency artifact. The exclusion policy is the
44-record HUMAN file in this experiment, derived only from explicit Core 0
exclusions in `WHITEPAPER.md` §15.

| Quantity | Result |
|---|---:|
| E0014 closure rules | 345 |
| Declared exclusion records | 44 |
| Excluded rules reached by the closure | 32 |
| Retained rules | 313 |
| Pruned excluded-rule edges | 27 |
| Unresolved reference edges retained | 216 |
| Unique unresolved reference names | 128 |
| Mechanically classified notation names | 13 |
| Names needing adjudication | 115 |
| Non-closed retained references | 0 |
| Independent projection difference | 0 |

The graph-level projection is closed. Every retained reference either points to
another retained rule or remains an explicitly recorded unresolved name. No
normative StandardIR rule was edited or deleted. The projected artifact is not
yet a complete grammar projection because the dependency artifact intentionally
does not retain enough RHS structure to prove that removing every excluded
alternative preserves the syntax tree shape.

The result narrows the next decision. The explicit exclusions can be projected
mechanically, but 115 unresolved names still need StandardIR adjudication. The
most visible groups are standard grammar metavariables and helper names such as
`actual-arg-spec-list`, `type-name` and `where-construct-name`. They are retained
as unresolved evidence rather than guessed into grammar rules.

Artifact: [R000001](../../../artifacts/runs/E0015/R000001-eligible.toml).
