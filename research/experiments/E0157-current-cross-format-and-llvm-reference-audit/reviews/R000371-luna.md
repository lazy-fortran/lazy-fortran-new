# Luna review R000371

## Question

How well does this represent the Fortran 2023 standard?

## Adjudication

PASS — bounded structural audit; OPEN for language equivalence.

The corrected audit counts only `r_* : $ =>` tree-sitter declarations as
grammar rules. It reports 664 rules, while the EBNF and ANTLR4 inventories each
report 659; the uppercase lexer declarations are excluded. Generated feature
presence is derived from StandardIR source rule IDs intersected with emitted
source-lineage metadata. All four generated formats cover 1,061/1,068 source
alternatives with seven explicit skips and zero missing alternatives, and all
11 audited features are present in their generated body columns.

Flang is now represented by a separate structural witness: the intersection of
the StandardIR feature rule IDs with its retained `R####` comments. The pinned
file contains 195 such comments, and two of the 11 feature rows intersect. This
is evidence about retained rule comments, not evidence that the other features
are absent from Flang's implementation.

The audit does not establish parser behavior, semantic correctness, diagnostics,
runtime behavior, or language equivalence. Bison generator acceptance and the
427/2,266 conflict inventory remain separate open parser-quality evidence.

Evidence reviewed:

- `.cache/runs/E0167-audit-replay-v2/summary.json`
- `.cache/runs/E0167-audit-replay-v2/inventories.tsv`
- `.cache/runs/E0167-audit-replay-v2/feature-matrix.tsv`
- `research/experiments/E0157-current-cross-format-and-llvm-reference-audit/analyse.sh`
- `.cache/runs/E0164/R000365-four-format-regeneration`
