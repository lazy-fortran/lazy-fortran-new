# Luna review R000324

Model: GPT-5.6 Luna

Prompt: how well does this represent the Fortran 2023 standard? Review the
corrected E0157 deterministic report and the current E0158 fidelity report;
check tree-sitter counting, source-lineage feature derivation, omissions and
the distinction between generator acceptance and equivalence.

## Adjudication

E0157 passes as a bounded structural audit of the selected `program` profile.
The corrected script counts only `r_*: $ =>` tree-sitter declarations, reports
664 heads, and derives generated feature presence from StandardIR `lhs`/rule
IDs intersected with emitted source-lineage metadata. The 1,068/1,068 source
identity result and all four generator statuses pass.

The report remains open for completeness: 1,061 of 1,068 bodies are emitted
and seven are explicit omissions. Reference feature columns are structural
head witnesses, not normative or behavioral evidence. Bison still reports
427 shift/reduce and 2,266 reduce/reduce conflicts, and generator acceptance
does not establish language equivalence.

The current E0158 report passes its five witnesses, all 522 source spans and
duplicate occurrences, all-record token/reference leaves, source hashes,
canonical-text/PDF manifest lineage, and the negative mutation. The 46
surface-normalization differences are explicitly reported as standard
optional-plus-ellipsis notation versus structural repetition, not silently
treated as identical text. The PDF gate is therefore trustworthy for its
declared source-fidelity scope; parser behavior and complete language
equivalence remain open.

## Decision

`E0157: PASS (bounded structural audit)`; `E0158: PASS (source-fidelity gate)`;
do not call either result a complete Fortran parser or language-equivalence
proof.
