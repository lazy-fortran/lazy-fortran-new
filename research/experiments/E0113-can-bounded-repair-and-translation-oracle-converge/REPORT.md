# E0113 report

This report is completed after the fixed local, cloud and Codex-Luna runs.
The protocol is defined in the manifest and D0052/D0053. Generated run
records remain under `.cache/runs/E0113/` and are not committed.

The central results are:

```text
discovery: 121 unresolved rows, accepted / abstained / hard-failed
translation control: 6 E0110 rows, exact / wrong-accepted / abstained / hard-failed
cost: total wall time, inference wall time, calls and repairs
```

No result is promoted into StandardIR by this experiment.
