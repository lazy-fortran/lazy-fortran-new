# Luna review of current selected source-backed grammar

Model: GPT-5.6 Luna

Inputs:

* E0147/R000022 selected `program` export and its source-backed StandardIR;
* current validator and Bison diagnostics;
* pinned LFortran `parser.yy` at `caf87b660f803148f000046392a5da803f9fc630`;
* E0149 current matrix and findings.

## Review

The selected export is a strong, traceable syntax projection, but it is not
yet evidence of complete Fortran 2023 parser or language conformance.

R741, R843, R1103, R1307, R1315 and R1416/R1417 are faithful in the current
output. The current output retains the complete procedure-component,
implied-do, association, edit-descriptor, position-edit and submodule forms;
R1416 duplicate occurrences retain merged lineage. These are not current
StandardIR defects.

The concrete target issue is four Bison-useless nonterminals and seven useless
rules: `r_array_section`, `h_r_R918_1`, `h_r_R918_2` and
`r_complex_part_designator`. R918 is substituted into the selected designator
path while the original normalized symbols remain unreachable. This is a
target-pruning and normalization-witness gap, not evidence that R918 is absent
from StandardIR.

The 427 shift/reduce and 2,266 reduce/reduce conflicts remain diagnostics, not
a conformance result. The audit still lacks language-equivalence corpus
evidence, complete lexer behavior, semantic/diagnostic behavior and
preservation witnesses for left-recursion, nullable/list lowering and the
R918/R915 factoring.

LFortran is parser-engineered: it has partial-unit entry points, flattened
expressions with precedence declarations, lexer-level formatting, executable
lexer/parser actions and an explicit conflict budget. Those are genuine
reference advantages, not reasons to rewrite normative StandardIR from its
production bodies.

StandardIR’s genuine advantages are source rule/page/byte/hash/lineage
provenance, preservation of normative role distinctions and a clean boundary
without copied LFortran actions or extensions. Both kinds of advantage must
remain visible in the comparison.
