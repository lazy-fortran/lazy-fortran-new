# E0096 report

All values and evidence below are regenerated by
`research/experiments/E0096-can-one-generic-evaluator-execute-a-nest/analyse.sh`.
The analyzer makes no experimental model calls.

## Question

Can one generic evaluator execute a nested `not`/`or` predicate over
intrinsic-type facts?

## Method

The analyzer reruns E0090 and selects C734 from its generated rule table. The
generated evaluator reads a derived-type name from three real Fortran module
sources and interprets `not`, `or`, `eq` and `intrinsic-type-name` generically.
It emits a source-linked acceptance or diagnostic record.

The witnesses use a permitted name, `DOUBLEPRECISION`, and `INTEGER`. GNU
Fortran and Flang independently check the same sources. Separate target and
witness oracles fix the predicate, source location and expected outcomes. A
predicate mutation is the negative control.

## Result

| Quantity | Result |
|---|---:|
| E0090 predecessor rule rows | 22 |
| Selected rule rows | 1 |
| Generic constructor forms | 4 |
| Independent target-oracle difference | 0 |
| Provenance matches | 1 |
| Fact matches | 1 |
| Evaluator cases | 3 |
| Evaluator cases accepted | 1 |
| Evaluator cases rejected | 2 |
| Behavioral compiler observations | 6 |
| Behavioral agreements | 3 |
| Diagnostic rows | 2 |
| Source-linked results | 3 |
| Parser projections | 0 |
| Model calls | 0 |
| Controlled mutation | `observed_failure` |

The generic evaluator agrees with both behavioral compilers on all three
witnesses. It preserves the C734 source link for the accepted and rejected
cases. This extends nested evaluation beyond E0095 while remaining a bounded
semantic slice.

Artifact: [summary](../../../artifacts/runs/E0096/R000001-summary.toml).
