# R000305 Luna final-size review of role-family factoring

Reviewed `standard-new` commit `a856182ad50bb23b2f5fd1c9cd77f19bb80e118e`
in `/home/ert/code/.wt/standard-new-role-specialization`.

## Verdict

Not ready only for size-cap violations.

Verified: complete before/after target-record and expression validation,
mutation controls, alternative order, chains, cycles, export self-validation,
all four formats, bounded token/reference/sequence/choice/optional/repeat
oracle with hard overflow guards, generic implementation without LFortran
copying, full `fo`, focused tests and diff checks.

Remaining new blockers:

- `standardir_grammar_target_support.f90` is 1,037 lines, above the local
  1,000-line module cap.
- `standardir_grammar_export_batch` is 131 lines, above the local 100-line
  procedure cap.

The D0011 text-policy finding at `standardir_grammar_closure.f90:526` is
unchanged and outside this slice.
