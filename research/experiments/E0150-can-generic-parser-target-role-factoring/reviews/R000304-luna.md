# R000304 Luna review of role-family witness validation

Reviewed `standard-new` commit `4cfce25a8096ebd1a99143a919163ae7adf2cd86`
in `/home/ert/code/.wt/standard-new-role-specialization`.

## Verdict

Not ready to merge.

## Verified

Witness validation now covers target expression, id, source occurrence,
alternative, origin, resolution, roles and provenance. Mutation controls,
export self-validation, alternative order, chains, cycles, all four formats,
generic/no-LFortran-copy checks and the full `fo` workflow pass. The
pre-existing text-policy finding at `standardir_grammar_closure.f90:526` is
outside the slice.

## Remaining blockers

1. A new short-circuit-reliance lint defect occurs at
   `standardir_grammar_target_support.f90:269`; split the guard before using
   `witness(1)`.
2. `standardir_grammar_factor_role_family` exceeds the local 100-line
   procedure cap.
3. The independent finite oracle evaluates only token/reference/sequence,
   silently treats recursion-depth overflow as success, and can therefore miss
   optional/repeat/choice or deeper recursive changes.
