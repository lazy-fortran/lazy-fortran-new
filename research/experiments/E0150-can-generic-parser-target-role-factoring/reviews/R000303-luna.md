# R000303 Luna review of repaired role-family factoring

Reviewed `standard-new` commit `6d28c71b86e2bfced7d673209f4be21a8d2328ac`
in `/home/ert/code/.wt/standard-new-role-specialization` against base
`dc75e7f4905e58d9b89d04c77f4f09223b57a579`.

## Verdict

Not ready to merge.

## Accepted fixes

Reordered accepted/rejected alternative lineage, alias chains, cycles,
witness mutations, all four format paths and the module-size cap are covered
and pass. The mechanism remains generic and does not copy LFortran grammar.
The full `fo` workflow and focused export test pass.

## Remaining blockers

1. Witness validation checks LHS and provenance but does not compare the
   target expression or all source/target identity fields: `id`, source
   occurrence, alternative, origin and resolution. The mutation tests do not
   cover those fields or a changed target expression.
2. The tests inspect emitted text but provide no independent bounded
   accepted/rejected language-preservation oracle for the transformation.
   Export self-validation is therefore limited by the incomplete witness.

The pre-existing D0011 text-policy finding at
`src/standardir_grammar_closure.f90:526` is outside this slice. The role
repair is not ready until the two correctness blockers are closed.
