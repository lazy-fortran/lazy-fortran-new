# Luna review of E0147 R000016

Status: qualified pass for the source-backed projection subgate

The exact pins in `metadata.tsv` are `standard-new` commit
`1cdd9490375199d93c755fb3c36bc9dcf2c285e6` and laboratory commit
`4cb9b42bda2a1421e195b47a84d579d09f1fc205`. The input has 522 syntax
objects, 1,223 production records and 1,068 source alternatives. Source
span, order, occurrence and duplicate checks report no errors.

All four projections are body-bound: 1,066 alternatives are covered, two
are explicitly skipped, and there are no missing mappings or header gaps.
The skipped records are the source-backed semantic-only `R401 xyz-list` and
`R403 scalar-xyz` alternatives, with their shared `xyz` dependency recorded.
The multi-lineage records are generic indirect-left-recursion expansions and
retain combined source ancestry. Provenance hashes are typed and every
projection carries the `MECHANICAL` origin label.

ANTLR4, Bison and tree-sitter accept the generated files, undefined-symbol
diagnostics are zero, and the source-removal negative control fails as
intended. EBNF is a projection-only output and has no independent parser
validator in this run.

The Bison output correctly contains `%glr-parser`. Bison reports 758
shift/reduce and 5,033 reduce/reduce conflicts in two warnings, but zero
useless-rule warnings. These are documented target-parser ambiguities, not
evidence of lost StandardIR structure. They prevent the Bison projection
from being called conflict-free, but do not fail the source-backed
projection subgate.
