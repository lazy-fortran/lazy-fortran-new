# Luna review of E0147 R000015

Status: PASS, qualified

The pinned run uses `standard-new` commit
`9a91aca57f7d7ab834f962b6b76fbb3b114ffa8e` and laboratory commit
`cdb852e4c70f8aad0b2e417d9d72790ea36bca23`. The SX input contains 522 syntax
records and 1,068 alternatives with consistent source hashing, spans, order
and duplicate accounting.

All four projections have body-bound provenance. The independent review found
1,066 retained alternatives with actual emitted expressions and no
comment-only occurrence. The repeated lineage identities are explained by
the generated expanded and left-recursion helper rules; repeated source rule
IDs have distinct source spans. Two alternatives are deliberately skipped as
semantic-only (`R401 xyz-list` and `R403 scalar-xyz`), and the run records
their disposition rather than claiming that all raw alternatives were
emitted.

ANTLR4, Bison and tree-sitter exit successfully. The source-projection
negative control fails as intended. EBNF remains a projection-only format and
has no independent parser validator in this run.

Qualification: Bison reports 758 shift/reduce and 5,033 reduce/reduce
conflicts, with 70 warning lines. This is retained target-projection evidence
under D0030 and is not evidence of a dropped StandardIR alternative, but it
must be explained before the selected grammar milestone is called fully
complete. No model or semantic experiment is unlocked by this review alone.
