# R000283 Luna review of selected-root replay

Date: 2026-08-15
Model: GPT-5.6 Luna
Mode: read-only independent review
Production commit: `32158f80662c37992a40369dd4011edad9414f51`
Run: `.cache/runs/E0147/R000019`

## Judgment

The selected-root export is a credible, broad, mechanically derived and
well-provenanced StandardIR projection. It does not close the selected-parser
gate.

## Findings

* The selected start symbol is correct and the six omitted declared roots are
  explicitly reported. The output retains `MECHANICAL` origin, target mode,
  rule/page/source-hash/byte-span provenance and source lineage.
* Source-graph reachability is not yet parser-level reachability. The log
  marks `array-section`, `h_r_R918_1`, `h_r_R918_2` and
  `complex-part-designator` as selected, while Bison reports four useless
  nonterminals and seven useless rules. This mismatch must be represented or
  fixed generically.
* The role expansion is compatible with D0092, but the replay lacks the
  required machine-checkable specialization and merged-lineage witness.
* Bison reports 427 shift/reduce and 3,357 reduce/reduce conflicts for the
  selected root. These are target metrics, not a conformance result; there is
  not yet a selected-parser conflict policy or independent behavior witness.
* The required companion lexer-contract projection is absent. Embedded token
  declarations are not by themselves the D0093 contract artifact.

## Conclusion

The source validity audit supports the StandardIR input, not language
equivalence of the selected Bison grammar. The next work is generic parser
reachability/disposition accounting, role-specialization provenance witnesses
and the companion lexer contract. No manual Fortran production repair is
justified by this review.
