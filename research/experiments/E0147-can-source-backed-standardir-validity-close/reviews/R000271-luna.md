# Luna review of E0147 R000009

Model: GPT-5.6 Luna
Date: 2026-08-15
Verdict: FAIL

## Scope

The review inspected the exact committed replay generated from
`standard-new` commit `e6a03b6b4ddf704c882dba93847062a8a635ba4c`, including the
source-backed `standardir.sx`, `classifications.sx`, `roots.sx`,
`lexical-facts-v0.sx`, and the EBNF, ANTLR4, Bison and tree-sitter outputs.

## What passed

The generator pin is correct. Source-span, occurrence, ordering and duplicate
checks report no errors. Every source rule and top-level alternative appears
in output annotations. ANTLR4, Bison and tree-sitter accept the generated
files, and the deliberate undefined-symbol mutation fails.

These are smoke and annotation checks, not a proof that the generated grammar
is source-faithful.

## Remaining failures

* Two of the three declared metanotation roots, `xyz-list` and `scalar-xyz`,
  are omitted from every projection. The generator reports only an aggregate
  skipped count and gives no root-level disposition.
* Indirect left-recursion normalization assigns the helper provenance from the
  first recursive alternative. The R901 witness therefore labels an expansion
  from the complex-part alternative as another alternative.
* The source-projection equivalence audit fails for all four formats. Existing
  validator output still reports PASS because it does not consume that audit.
* Generated headers contain no required `MECHANICAL` origin label.
* Canonical-text and source-document/PDF hashes remain serialized under the
  same untyped `source-sha256` field.
* Unicode lexical facts are emitted as dead wrappers while grammar productions
  use literal characters, so lexical closure is not demonstrated.
* The external parser tools accept the files but provide no positive Fortran
  behavior check. Bison reports 751 shift/reduce and 4,141 reduce/reduce
  conflicts in this replay.

E0147 remains open. No semantic or model experiment may consume this output as
normative grammar.
