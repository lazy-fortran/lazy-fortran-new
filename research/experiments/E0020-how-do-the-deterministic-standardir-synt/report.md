# E0020 report

All counts and the summary hash below are regenerated by
`research/experiments/E0020-how-do-the-deterministic-standardir-synt/analyse.sh`.
The StandardIR input is pinned E0013 complete-core data. The adapter and
experiment definition are in `lazy-fortran-new` at commit `24d3b26`.

| Source representation | Source rules | Shared normalized names | StandardIR-only | Source-only |
|---|---:|---:|---:|---:|
| house `standard` `.g4` | 56 | 29 | 473 | 27 |
| kaby76 `.g4` | 646 | 485 | 17 | 161 |
| LFortran `parser.yy` | 240 | 27 | 475 | 213 |
| Flang rule comments | 460 | 460 | 42 | 0 |

These values come from the TSV written by the analysis command. A separate
awk traversal reproduced the house-grammar overlap and Flang overlap, and the
fixed witnesses `program-unit` and `R501` were present. The independent
difference was zero.

This is a structural inventory comparison, not a claim of grammar
equivalence. The source formats expose different naming and decomposition
choices: the house and kaby76 inputs are ANTLR productions, LFortran is a
Bison rule file, and Flang is compared through its rule-ID comments. The
normalized sets therefore identify candidates for adjudication; they do not
decide whether StandardIR or a comparison source is correct. Duplicate
StandardIR lhs records and unresolved references remain explicit follow-up
work. No GPL implementation source was read; gfortran is reserved for a
separate behavioral adapter.

Artifact: [R000001](../../../artifacts/runs/E0020/R000001-structural-comparison.toml).
