id: "E0146"
title: "Generic TargetIR source-origin query"
question: >
  Can TargetIR provide a generic source-origin query over normalized records?
status: reported        # draft | running | reported | abandoned
opened: "2026-08-14"
closed: "2026-08-14"
reported_run: R000258
reported_runs: [R000258]

# Every commit the result depends on. Exact, not "main".
repos:
  lazy-fortran-new: "4a2619d"
  fortback-new: "a149015"

# What goes in. Pinned artifacts by manifest name; profiles by ID.
inputs:
  standard: j3-24-007
  profile: targetir-v0

# The axes being varied. Omit the key if there is only one condition.
variables:
  sources: [riscv, aarch64, mixed]
  controls: [blank_origin, undersized_output]

# Explain whether model names form a controlled family ladder or separate
# controls. Pin revisions and runtime settings before the first run.
model_selection: "No model calls; deterministic source-preserving TargetIR query."

# Name these before running anything. Choosing a metric after seeing the data
# is how an experiment becomes a demonstration.
metrics:
  - source-origin indices preserve insertion order
  - mixed-source query results preserve identity
  - invalid origin and capacity controls reject transactionally
  - fo warnings

# State the complete eligible set and exclusions before the first run. A rate
# over the successful subset is reported alongside the strict rate.
denominator: "Independent RISC-V, AArch64, mixed-source and invalid-input witnesses."

# State the independent behavioral or structural oracle. Self-consistency is
# not an oracle.
independent_oracle: "Independently authored expected indices and invalid-input controls plus the full fortback-new fo gate."

# Record versions needed to reproduce each run.
toolchain:
  record_in_run: true
  fields: [compiler, fo, poppler, oracle_versions]

# What result would disappoint you? If nothing would, this is not an experiment.
falsifiable_by: "A source query changes order or identity, accepts invalid origin/capacity input, or full fo fails."

# The condition that closes the experiment and the evidence it must report.
gate: "All source and control witnesses pass transactionally and full fo passes with zero warnings."

# Script that turns runs into the tables and figures in report.md.
analysis: "Run the production test and full fo; record the immutable result in research/runs/2026-08.jsonl."
