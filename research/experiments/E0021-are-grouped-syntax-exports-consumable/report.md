# E0021 report

All values below are regenerated by
`research/experiments/E0021-are-grouped-syntax-exports-consumable/analyse.sh`.
The generator is `standard-new` at commit `7344c65`; the validation adapter is
in `lazy-fortran-new` at commit `8d57573`.

| Target | Definitions | Duplicate definitions | Validator status | Unresolved result |
|---|---:|---:|---:|---|
| ANTLR4 | 502 | 0 | 1 | 181 unique undefined rule names |
| Bison | 502 | 0 | 1 | 181 unique undefined symbol names |
| tree-sitter | 502 | 0 | 1 | first undefined symbol `r_xyz` |

The analysis command regenerated four ignored exports from 522 StandardIR
records. Its independent source traversal found 502 unique lhs names and 20
repeated records. The generated ANTLR, Bison and tree-sitter definition scans
found 502 definitions in each format and zero duplicate definitions. The
validator status `1` is therefore not a duplicate-rule failure.

All three target tools reject the projection because the selected complete-core
syntax input still contains references whose lexical or full-source definitions
are outside this input. ANTLR4 and Bison expose 181 such names; tree-sitter
stops at the first, `r_xyz`. This is a retained verification failure, not a
claim that the partial syntax export is a usable parser. The EBNF export is
generated but has no validator in this run. Behavioral comparison against
LFortran, Flang and gfortran remains blocked until the lexical/token boundary
and the selected-source reference closure are represented.

Artifact: [R000001](../../../artifacts/runs/E0021/R000001-export-validation.toml).
