# E0016 report

All counts, hashes and membership sets below are regenerated by
`research/experiments/E0016-does-standardir-syntax-project-mechanica/analyse.sh`.
The input is the pinned E0013 complete-core StandardIR artifact. The generator
is `standard-new` at commit `427df73`; the checker and experiment definition
are in `lazy-fortran-new` at commit `6392816`.

| Quantity | Result |
|---|---:|
| StandardIR syntax records | 522 |
| EBNF productions | 522 |
| Provenance comments | 522 |
| Source-hash matches | 522 |
| Production-order difference | 0 |
| Fixed structural witnesses | pass |
| Model calls | 0 |
| Output bytes | 112290 |
| Output SHA-256 | `8dd3f8eec8196691a17ecd3b8e93029bbeb942e0036fc061c0f6e01db206fe59` |

The canonical EBNF projection passed. The independent awk pass extracted the
ordered rule, lhs and source metadata from the input and found exact agreement
with the generated provenance comments and productions. The fixed witnesses
cover a comma-separated repetition, a zero-or-more repetition and an
alternative production.

This establishes one syntax export from StandardIR. It does not establish
ANTLR, Bison, tree-sitter or specialized-parser equivalence, and it says
nothing about semantic constraints or parser acceptance. Those remain separate
comparison and adjudication work.

Artifact: [R000001](../../../artifacts/runs/E0016/R000001-ebnf.toml).
