id: "E0164"
title: "Source-backed lexer contract and parser behavior gates"
question: >
  Can source-backed lexer contracts and generated grammar behavior pass
  independent lexer, generator, and reference-corpus gates?
status: reported            # draft | running | reported | abandoned
opened: "2026-08-15"
closed: "2026-08-15"
reported_run: R000348

# Every commit the result depends on. Exact, not "main".
repos:
  lazy-fortran-new: "5abdc784d4fbac652a4c6c3f8b660cb3f1a877cf"
  standard-new: "7d011f49e0e74e95b88a90c5894ea3358fc5ee82"
  fortfront-new: "d6de5f47afa8870c7e1dbec5dff06ec0ba79f7a3"
  oracle-lfortran: "caf87b660f803148f000046392a5da803f9fc630"
  oracle-flang: "cff4ca2e51f89c992d8a1b6ce1067846ba680e2e"
  oracle-gcc: "395e3d8131c189cd58e8c8061cdc77d1c44e3822"

# What goes in. Pinned artifacts by manifest name; profiles by ID.
inputs:
  standard: j3-24-007
  profile: selected-program
  baseline_run: E0161/R000338
  candidate_run: E0160/R000337
  lexical_facts: ../standard-new/specs/lexical-facts-v0.sx
  standard_manifest: artifacts/standards/j3-24-007.toml
  external_behavior: E0041/R000050
  bounded_language: E0161/R000340

# The axes being varied. Omit the key if there is only one condition.
variables:
  projection: [baseline, role-family-candidate]
  lexer_contract_origin: [MECHANICAL]
  parser_runtime: [not-yet-integrated]

# Explain whether model names form a controlled family ladder or separate
# controls. Pin revisions and runtime settings before the first run.
model_selection: "No model calls in the gate. The mandatory Luna review is
an independent read-only adjudication of the generated report, not a
translation or repair step."

# Name these before running anything. Choosing a metric after seeing the data
# is how an experiment becomes a demonstration.
metrics:
  - lexer_contract_rows
  - lexer_contract_provenance
  - four_format_generator_status
  - lexical_positive_and_negative_mutation_status
  - fortfront_lexer_runtime_tests
  - bounded_language_positive_cases
  - bounded_language_negative_cases
  - external_reference_corpus_agreement
  - generated_parser_runtime_status
  - model_calls

# State the complete eligible set and exclusions before the first run. A rate
# over the successful subset is reported alongside the strict rate.
denominator: >
  Both selected-program grammar projections, every row in each emitted lexer
  contract, all four generated grammar formats, the complete E0161 bounded
  corpus of 359 positive and 636 negative cases, the ten-case E0041 reference
  corpus, and all 24 fortfront-new tests at the pinned production commit.

# State the independent behavioral or structural oracle. Self-consistency is
# not an oracle.
independent_oracle: >
  A separate Python adjudicator checks contract lineage and row integrity;
  E0147 and E0156 provide generator and lexical mutation gates; fortfront-new
  supplies the generic lexer runtime tests; E0161 supplies an independent
  bounded grammar recognizer; and E0041 supplies three independent external
  frontend behavior oracles. No generated parser-runtime equivalence is
  inferred from these components.

# Record versions needed to reproduce each run.
toolchain:
  record_in_run: true
  fields: [compiler, fo, poppler, oracle_versions]

# What result would disappoint you? If nothing would, this is not an experiment.
falsifiable_by: >
  A contract row lacks mechanical origin or complete source provenance, the
  two projections disagree, any generator or negative mutation gate fails,
  fortfront-new tests fail, the bounded corpus changes, the external behavior
  matrix disagrees, or a supposedly integrated generated parser runtime is
  absent. The last condition is reported as an open gate rather than hidden.

# The condition that closes the experiment and the evidence it must report.
gate: >
  The source-backed lexer contract, all four generator projections, lexical
  mutation controls, generic fortfront lexer tests, and positive/negative
  reference behavior witnesses must pass. A complete E0164 closure additionally
  requires an executable generated parser runtime consuming the selected grammar;
  the existing fortfront lexer module alone does not satisfy that condition.

# Script that turns runs into the tables and figures in report.md.
analysis: >
  research/experiments/E0164-can-source-backed-lexer-contracts-and-ge/analyse.py
  <baseline-dir> <candidate-dir> <lexical-facts> <standard-manifest>
  <bounded-language-report> <external-summary> <fortfront-test-log> <output>
  [--lexer-test-count N --runtime-test-log FILE --runtime-test-count N]

replays:
  - run: R000348
    kind: lexer-contract-generator-and-behavior-gate
    report: .cache/runs/E0164/R000348/report.tsv
  - run: R000349
    kind: generic-parser-runtime-slice-replay
    report: .cache/runs/E0164/R000349/report.tsv
