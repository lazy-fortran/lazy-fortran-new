# E0019 report

All counts and hashes below are regenerated by
`research/experiments/E0019-does-standardir-syntax-project-mechanica/analyse.sh`.
The input is the pinned E0013 complete-core StandardIR artifact. The generator
is `standard-new` at commit `27406a7`; the checker and experiment definition
are in `lazy-fortran-new` at commit `b3046ef`.

| Quantity | Result |
|---|---:|
| StandardIR syntax records | 522 |
| Tree-sitter productions | 522 |
| Provenance comments | 522 |
| Source-hash matches | 522 |
| Lhs-mapping difference | 0 |
| Fixed structural witnesses | pass |
| Model calls | 0 |
| Output bytes | 138394 |
| Output SHA-256 | `bd470be7859d660c179a73cca6764a47140dd12c87c4e0f4104abc51e384afc5` |

The deterministic grammar.js projection passed. The independent awk pass
extracted ordered rule and source metadata from the input, applied the
tree-sitter-safe lhs mapping, and found exact agreement with generated
comments and declarations. Fixed witnesses cover a comma-separated sequence
and the program-unit repetition.

This establishes the requested tree-sitter syntax export. It does not establish
parser acceptance, lexer equivalence, duplicate-lhs normalization, unresolved
reference adjudication or agreement with external grammar corpora. Those remain
comparison work rather than being silently inferred from the export count.

Artifact: [R000001](../../../artifacts/runs/E0019/R000001-treesitter.toml).
