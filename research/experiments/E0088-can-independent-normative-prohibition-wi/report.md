# E0088 report

All values and evidence below are regenerated by
`research/experiments/E0088-can-independent-normative-prohibition-wi/analyse.sh`.
The analyzer makes no experimental model calls.

## Question

Can independent normative prohibition witnesses adjudicate the disputed C734
predicate without a model or a parser projection?

## Method

The analyzer reruns the E0081 Core 0 inventory and selects C734 exactly once.
It then checks three separately recorded normative source witnesses: C752,
C824 and C952. Each repeats the bounded construction `shall not be of type A
or B`. The independent oracle requires the target and witness phrases, source
locations and source hash to match the canonical J3/24-007 text, and requires
the `not(or ...)` candidate.

Two compiler frontends are used only as a secondary behavioral check. They
compile one ordinary derived type and reject derived type names that are
intrinsic type names. The compiler result does not replace the normative
witness gate.

## Result

| Quantity | Result |
|---|---:|
| Eligible Core 0 constraints | 287 |
| Target rows | 1 |
| Independent witness rows | 3 |
| Source-hash references | 6 |
| Target source-evidence matches | 3 |
| Witness source-evidence matches | 3 |
| Selected candidate | `not-or` |
| Disputed rows remaining in this slice | 0 |
| Independent oracle difference | 0 |
| Behavioral compilers | 2 |
| Valid controls accepted | 2 |
| Intrinsic-name controls rejected | 4 |
| Behavioral difference | 0 |
| Parser projection records | 0 |
| Model calls | 0 |
| Controlled mutation | `observed_failure` |

The disputed C734 row can therefore be resolved to:

```text
(not (or (eq type-name DOUBLEPRECISION)
         (intrinsic-type-name type-name)))
```

E0088 establishes this one bounded cross-clause normalization. It does not
claim that arbitrary English negation is mechanically formalized. The E0087
composite run remains immutable; a later composition run must incorporate this
successor state before the predicate contributes generated semantic wiring.

Artifact: [summary](../../../artifacts/runs/E0088/R000001-summary.toml).
