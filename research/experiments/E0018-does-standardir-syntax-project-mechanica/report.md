# E0018 report

All counts and hashes below are regenerated by
`research/experiments/E0018-does-standardir-syntax-project-mechanica/analyse.sh`.
The input is the pinned E0013 complete-core StandardIR artifact. The generator
is `standard-new` at commit `537614b`; the checker and experiment definition
are in `lazy-fortran-new` at commit `2040d53`.

| Quantity | Result |
|---|---:|
| StandardIR syntax records | 522 |
| Bison top-level productions | 522 |
| Provenance comments | 522 |
| Source-hash matches | 522 |
| Lhs-mapping difference | 0 |
| Helper productions | 695 |
| Fixed structural witnesses | pass |
| Model calls | 0 |
| Output bytes | 150859 |
| Output SHA-256 | `a2b6337fd621d57f99e3fb89219cc669fffc14c122829ade2996dddba2051707` |

The Bison projection passed. The independent awk pass extracted ordered rule
and source metadata from the input, applied the Bison-safe lhs mapping, and
found exact agreement with generated comments and declarations. Optional and
repeated expressions are represented by deterministic helper nonterminals;
the fixed witnesses cover the nested sequence in R401 and the program-unit
repetition in R501.

This establishes a Bison syntax export from StandardIR. It does not establish
lexer equivalence, parser acceptance, tree-sitter or specialized-parser export,
semantic constraints or agreement with external grammar corpora. Those remain
separate work.

Artifact: [R000001](../../../artifacts/runs/E0018/R000001-bison.toml).
